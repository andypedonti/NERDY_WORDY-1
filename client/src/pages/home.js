import React from 'react';
import DisplayBox from '../components/displayBox'

const Home = (props) => {
  return (<div>
    <DisplayBox />
  </div>
  );
};

Home.propTypes = {};

export default Home;
