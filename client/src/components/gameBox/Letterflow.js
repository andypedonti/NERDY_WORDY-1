// import React from 'react'

// const Letterflow = (props) => {
//     return (
//       <p className="Letterflow" id={`items${props.id}`} style={props.style}>
//           *
//       </p>  
//     )
// }

// class ABCs extends React.Component {
//     ABCs=()=>{
//         let animationDelay = "0s";
//         let fontSize = "100px";
//         let arr = Array.from(ABCs)

//         return arr.map((el, i)=>{
//             animationDelay = 
//             `${(Math.random()*26).toFixed(2)}s`;
//             fontSize = `${(Math.floor(Math.random()*10) + 10)}px`;
//             let style = {
//                 animationDelay,
//                 fontSize
//             }
//             return (<letterflow key={i} id={i} style={style}/>)
//         })
//     }
// }
// export default Letterflow;
